# Development Log

This log records the use of tools, prompts, changes, outputs, and lessons learned during the development of the Assignment 2 submission.

## Log Table

| No. | Date | Task | Tool Used | Prompt (summary or snippet) | Changes made | Output quality (your observation) | What you learned |
|---|---|---|---|---|---|---|---|
| 1 | 2026-04-16 | Create the initial architecture document | ChatGPT (GPT-5.4 Thinking) | "Design a client-oriented architecture for RideNow using bounded contexts, hexagonal architecture, notifications, eventual consistency, routing, timing rules, and node-level configuration." | Produced the first full draft of the RideNow architecture proposal in markdown. Defined the client-oriented principle, identified the main service-nodes, described responsibilities and owned data, and outlined happy and failure paths. | Strong initial output. The structure was coherent and aligned well with the assignment topic, but some service boundaries still needed refinement. | A strong first draft is useful for shaping the overall architecture, but bounded-context boundaries often need iteration. |
| 2 | 2026-04-16 | Refine the customer-facing coordination model | ChatGPT (GPT-5.4 Thinking) | "Clarify the role of the Broker so that customer actions enter through one main coordination point and customer-relevant updates return through the same point." | Strengthened the role of the Broker as the single customer-facing coordination service. Reduced ambiguity about which internal services communicate directly with the customer. | Improved clarity significantly. The architecture became easier to explain and more client-oriented. | In a client-oriented design, one main coordination service improves usability and simplifies the external model. |
| 3 | 2026-04-16 | Remove unnecessary service complexity | ChatGPT (GPT-5.4 Thinking) | "Reassess whether a separate Support node is really necessary given the Broker-centric model." | Removed the Support Node from the architecture and reassigned issue and complaint handling to a flow involving Broker plus the relevant domain services such as Payment and Tracking. | Better than the previous version. The architecture became leaner and more defensible. | Not every possible bounded context needs to become a separate service. Simplicity improves the quality of the proposal. |
| 4 | 2026-04-16 | Add routing as a first-class capability | ChatGPT (GPT-5.4 Thinking) | "Introduce a Route node that links the driver location, pickup point, and drop-off point and selects the fastest route." | Added the Route Node as a dedicated bounded capability. Extended the model to include route selection, ETA, distance estimation, and route recalculation. | Strong improvement. The platform became more realistic and mathematically grounded. | Routing is not a minor helper function in a ride-hailing system; it is a core capability with its own logic and state. |
| 5 | 2026-04-16 | Add temporal and operational policy handling | ChatGPT (GPT-5.4 Thinking) | "Make timeouts, intervals, and thresholds part of the architecture and externalise them into node-level configuration." | Added a temporal model, driver location update policy, and node-level externalised configuration for timing windows, retries, and thresholds. Chose node-level configuration rather than a separate Policy Node. | Strong and practical. The proposal became more operationally realistic. | Architectural quality is improved when operational constants are treated as configuration rather than hard-coded logic. |
| 6 | 2026-04-16 | Integrate ROS 2-inspired communication ideas carefully | ChatGPT (GPT-5.4 Thinking) | "Explain that the system is formally microservices, but consciously borrows selected ROS 2 communication ideas to strengthen node boundaries and data-driven behaviour." | Updated the introduction and architectural style to explain that the platform remains a microservice system while using a graph-of-service-nodes perspective inspired by ROS 2 communication concepts. | Strong final framing. It preserved the language of the module while showing a more advanced design influence. | Borrowing ideas from another architectural tradition works best when it strengthens the model without displacing the main vocabulary expected by the assignment. |
| 7 | 2026-04-16 | Produce the cleaned architecture proposal document | ChatGPT (GPT-5.4 Thinking) | "Generate a clean markdown document titled 'RideNow Ride-Hailing Platform Architecture Proposal' without author remarks or process commentary." | Produced the cleaned markdown version of the architecture proposal, including introduction, architectural style, node catalogue, graph model, hexagonal structure, timing model, events, flows, deployment, and testing strategy. | High quality final output. Clean, coherent, and ready to include in the submission pack. | Final presentation quality matters. Removing process commentary makes the document more academic and submission-ready. |
| 8 | 2026-04-16 | Generate the high-level architecture diagram in code | ChatGPT (GPT-5.4 Thinking) | "Create a high-level architecture diagram for RideNow in PlantUML code, showing Broker, Notification, Driver, Route, Pricing, Payment, Tracking, and the client-facing flow." | Generated PlantUML code for the high-level architecture. The code was then rendered externally into the final PNG file `High-Level_RideNow_Architecture.png`. | Useful structure, but the final visual quality depended on rendering and layout adjustments outside the chat. | Text-based diagram generation is useful, but rendered output still needs human review. |
| 9 | 2026-04-16 | Generate the happy-path event flow diagram in code | ChatGPT (GPT-5.4 Thinking) | "Create a PlantUML sequence/event-flow diagram for the RideNow happy path from ride request to payment confirmation." | Generated PlantUML code for the happy-path event flow. The code was rendered externally into the final PNG file `RideNow_Happy_Path_Event_Flow.png`. | Good logical sequence and useful for showing event-driven behaviour across services. | Sequence diagrams are especially useful for explaining event-driven architecture and one complete system flow. |
| 10 | 2026-04-16 | Update the architecture proposal to include final rendered diagrams | ChatGPT (GPT-5.4 Thinking) | "Update the architecture proposal so that it embeds the final PNG diagrams rather than raw diagram code." | Updated the main architecture proposal to reference the final rendered PNG files for Figure 1 and Figure 2 and added short explanatory captions for both. | Strong improvement in document quality and professionalism. | Submission documents should include finished artefacts rather than intermediate code when the final output is intended for assessment. |
| 11 | 2026-04-16 | Update the development log to reflect diagram work and document refinement | ChatGPT (GPT-5.4 Thinking) | "Expand the development log with the work on the two diagrams and the subsequent document update." | Added specific rows covering diagram generation, rendering, and integration into the architecture proposal. | Better coverage of actual work performed. More aligned with the assignment requirement to log significant GenAI use. | A useful development log should record not only code generation but also important design and documentation iterations. |
| 12 | 2026-04-18 | Evaluate the first implementation attempt and record a corrective reset after discovering scope drift | ChatGPT (GPT-5.4 Thinking) | "Review the generated RideNow implementation against the prompt and correct anything that would allow another Broker-only shortcut implementation to pass as complete." | During evaluation of the first full implementation attempt, it became clear that the generated system had collapsed into a Broker-centric slice and that only one real service runtime had been built, while the other required services were missing as real executable boundaries. The approach had also relied on Broker-local shortcuts instead of real inter-service flows. This meant a full working day had effectively been spent on an under-scoped result. As corrective action, the repository was rolled back to the pre-implementation baseline and the prompt was reopened for hardening before a second attempt. | The evaluation exposed a serious quality failure: the generated system was technically active in parts but did not satisfy the assignment scope or the intended architecture. | Final evaluation must check architectural completeness and real collaboration between services, not just whether some code runs or some tests pass. |
| 13 | 2026-04-18 | Harden the prompt to stop another Broker-only collapse before the second attempt | ChatGPT (GPT-5.4 Thinking) | "Rewrite the governing prompt so that the agent cannot treat a Broker-only vertical slice as an acceptable completion of RideNow." | Updated the prompt to require: top-down outside-in TDD from full acceptance/integration scenarios to the narrowest unit tests; all seven RideNow services as separate production runtime boundaries; explicit prohibition of Broker-only shortcuts and simulated replacements for required services; real inter-service verification through the event backbone; and hard-stop conditions if implementation drifts toward a collapsed architecture. | Strong corrective improvement. The revised prompt is much less permissive about under-scoped implementations and much more explicit about what counts as completion. | If a project requires architectural completeness, that completeness must be written into the prompt as a non-negotiable constraint, not left to inference. |
| 14 | 2026-04-18 | Add operational and manual-demo constraints to the prompt before the second attempt | ChatGPT (GPT-5.4 Thinking) | "Make the final system startable and stoppable from one top-level command each, and ensure it is manually testable with dummy/demo data." | Extended the prompt with operational requirements for the final system: one documented top-level local `start` command/script, one documented top-level local `stop` command/script, centralised stack control rather than per-service manual startup, and a requirement that the `start` path leaves the platform in a demo-ready state with dummy/demo data loaded or bootstrapped for manual end-to-end verification. | Strong practical improvement. The expected deliverable is now not only architecturally complete but also directly operable by a human evaluator. | Turnkey delivery must include operational usability. If startup, shutdown, and demo preparation are not explicit requirements, the result may be technically buildable but still poor to evaluate manually. |
| 15 | 2026-04-18 | Add a mandatory full-system integration-first starting point to the prompt before the second attempt | ChatGPT (GPT-5.4 Thinking) | "Require the next run to begin by proving the full connectivity of all services through integration tests covering every main operating mode." | Added a new prompt rule that development must begin with failing full-system integration/acceptance tests proving the complete service graph and its operating modes before deep service-local implementation begins. The prompt now names the minimum operating-mode catalogue explicitly: request-ride-acknowledgement, driver-assigned, route-and-eta-feedback, payment-authorised, trip-progress, ride-completed-payment-confirmed, no-driver-available, payment-failed, issue-submitted, stack-startup-readiness, stack-shutdown, and manual-demo-mode. | Strong methodological improvement. The second attempt is now forced to start from whole-system connectivity rather than from isolated service internals. | When a distributed system is the actual deliverable, the first meaningful proof should be whole-system integration across named operating modes. Otherwise, local progress can hide system-level incompleteness for too long. |
| 16 | 2026-04-18 | Start the second implementation attempt from a clean baseline | Codex (GPT-5.4) plus terminal tooling (`git`, `pytest`, PowerShell) | "Run the hardened prompt from scratch and stay on the full-system integration-first track." | Bootstrapped the repository again from the pre-implementation baseline. Wrote `docs/analysis/reference_analysis.md`, `PLAN.md`, and `docs/adr/ADR-001-language-and-stack.md`, then set up the working sequence of phases and gates before writing production code. | Much stronger than the first attempt because the run started with an explicit plan and constraints rather than with ad hoc implementation momentum. | A reset is only useful if the second run begins with new control points. Reusing the old pace without a new plan would have recreated the same drift. |
| 17 | 2026-04-18 to 2026-04-19 | Build the skeleton and prove full-system topology before deep implementation | Codex (GPT-5.4), `pytest`, FastAPI test tooling, Dockerfile/Compose editing | "Start from failing full-system startup and operating-mode integration tests, then wire the minimum topology needed to make the first matrix pass." | Completed Phase 1 and Phase 2, then added the full-system red baseline in Phase 2A: startup/readiness matrix, happy-path connectivity slice, failure-path connectivity slice, and manual-demo-mode slice. Implemented the first minimal seven-service topology so the system could prove real service connectivity before user-story work began. | High-value milestone. This materially corrected the main error of the first attempt by making all seven services visible in tests before story development expanded. | In a multi-service assignment, topology must be proven early. If all seven service processes are not visible in the first red-green loop, later progress can still hide a collapsed design. |
| 18 | 2026-04-19 | Introduce shared contracts and in-memory adapters for safe outside-in story work | Codex (GPT-5.4), `pytest`, contract-test harnesses | "Define outbound ports and contract suites before implementing the story graph in detail." | Added shared outbound ports, failing contract suites, and in-memory adapters for service-to-service communication. This created a stable base for iterating through the acceptance catalogue while still working from the outside inward. | Good quality and low-risk. The abstraction layer remained justified because it was driven by contract tests rather than by speculative generality. | Contract suites are useful when they are used as a common executable agreement across both in-memory and real adapters; otherwise they become accidental complexity. |
| 19 | 2026-04-19 to 2026-04-20 | Implement the full user-story catalogue across seven collaborating services | Codex (GPT-5.4), `pytest`, application code generation and refactoring through repeated TDD cycles | "Close Phase 4 story by story without falling back to Broker-only shortcuts." | Implemented request acknowledgement, driver assignment, route and ETA feedback, payment authorisation, trip progress, ride completion and payment confirmation, no-driver-available, payment-failed, and issue submission. The runtime behaviour was expressed as event-driven collaboration among `broker`, `driver`, `route`, `pricing`, `payment`, `tracking`, and `notification`, not as internal Broker-only update endpoints. | Strongest core implementation phase of the second attempt. The resulting system finally matched the architecture rather than only borrowing its vocabulary. | The acceptance catalogue is only meaningful when each story is backed by real service collaboration. A Broker status mutation is not a substitute for a distributed flow. |
| 20 | 2026-04-20 | Add a second inbound adapter without breaking the shared composition root | Codex (GPT-5.4), `pytest`, CLI/runtime wiring | "Prove that the Broker can be driven by more than one inbound adapter while preserving the same application core." | Added a CLI adapter for representative Broker commands and extracted a shared runtime/composition root so HTTP and CLI both drove the same application logic. | Good architectural outcome. This showed that the Broker boundary was genuinely hexagonal rather than tightly coupled to HTTP. | Adapter diversity is only worth documenting when the adapters truly share one application core; otherwise it is just duplication behind different entrypoints. |
| 21 | 2026-04-20 to 2026-04-21 | Replace in-memory infrastructure with real RabbitMQ and PostgreSQL, then close runtime deployment gates | Codex (GPT-5.4), `pytest`, Docker Compose, RabbitMQ, PostgreSQL, Kubernetes manifests | "Move from in-memory collaboration to real outbound adapters and deployable runtime assets." | Added real RabbitMQ adapters and PostgreSQL state stores with contract and integration tests, then wired the real runtime in Compose and added Kubernetes manifests. During this phase a local host-port conflict on PostgreSQL was discovered; the exposed host port was moved to `15432` while container-side service wiring remained unchanged. | Strong and practical. The service graph now ran against real infrastructure rather than only against test doubles. | Local infrastructure problems often appear mundane, but explicitly resolving them in code and configuration is part of making a project genuinely turnkey. |
| 22 | 2026-04-21 | Add observability, non-functional checks, and full-system verification gates | Codex (GPT-5.4), `pytest`, Prometheus, structured logging, coverage tooling | "Close the system only after smoke, observability, and non-functional gates are green." | Added E2E smoke tests, structured JSON logging, metrics, Prometheus wiring, health/readiness probes, load/resilience/security monitoring checks, and a full coverage gate. This moved the repository from "it works functionally" to "it is observable and testable as a system." | High quality completion work. The project became far easier to inspect, diagnose, and evaluate manually. | Non-functional work should not be treated as polish. For a distributed system, readiness, metrics, and resilience checks are part of the deliverable. |
| 23 | 2026-04-21 to 2026-04-22 | Finish documentation, one-command operations, and turnkey verification | Codex (GPT-5.4), MkDocs, PowerShell scripts, `kind`, `kubectl`, fresh-clone testing | "Make the repository runnable, testable, and explainable from a fresh clone with one-command start and stop." | Completed the documentation set, generated the MkDocs site, added `scripts/start.ps1` and `scripts/stop.ps1`, and validated the repository from a clean clone. Verified Compose startup/shutdown, all major test suites, and local Kubernetes deployment through `kind` plus `kubectl apply -k` and broker smoke validation by port-forwarding. | Strong final packaging. The project became much closer to the level of operational completeness expected from a turnkey submission. | A project is not fully done when the source compiles in the main working tree. It is done when a fresh clone can build docs, run tests, start the stack, and verify live behaviour. |
| 24 | 2026-04-22 | Fix the startup race that prevented reliable one-command demo bring-up | Codex (GPT-5.4), PowerShell, Docker Compose, live manual verification | "One-command startup must leave the system in a demo-ready state rather than merely issuing container start commands." | Diagnosed that application services could begin before RabbitMQ and PostgreSQL were truly ready, which made the one-command startup brittle. Corrected `scripts/start.ps1` so infrastructure comes up first, waits for readiness, then starts the application services and finally waits for `/ready` across the full service set before printing demo instructions. | Important quality fix. The script moved from optimistic orchestration to deterministic operational behaviour. | A top-level `start` command is only meaningful if it encapsulates readiness, not just process launch. Otherwise the operator still carries the timing burden manually. |
| 25 | 2026-04-22 | Stabilise non-functional dependency auditing in the Windows host environment | Codex (GPT-5.4), `pytest`, PowerShell, `pip-audit` | "Keep the non-functional gate deterministic even when host temporary-file handling is awkward." | Identified that the dependency-audit test could fail because of temporary-directory behaviour on the Windows host. Adjusted the non-functional test support so audit artefacts are written to a stable repository-local temporary location rather than relying on fragile host temp handling. | Good corrective maintenance. The fix reduced environment-specific noise without weakening the actual security check. | Non-functional gates need the same engineering discipline as functional tests. A flaky audit check is still a test bug that must be solved, not ignored. |
| 26 | 2026-04-22 | Repair documentation and add a practical manual integration-testing workflow | Codex (GPT-5.4), MkDocs, Bash helper script, live HTTP verification | "Make the final docs usable by a human tester in both Git Bash and PowerShell, and provide one practical manual integration script." | Added `scripts/integration_manual_test.sh` and the guide `docs/guides/manual_integration_testing.md`, then updated `README.md`, `running_locally.md`, `kubernetes_demo.md`, and the operations runbook. Fixed broken shell examples that mixed `cmd`, PowerShell, and Bash syntax, added explicit guidance for `customer-demo`, `customer-no-driver`, and `customer-payment-fail`, and added a `trace` mode to reveal the observed customer-visible status path during manual testing. | High practical value. The repository became much easier to verify manually, and the documentation no longer relies on shell syntax that only works in the wrong terminal. | Good documentation must be executable in the reader's real shell. A broken example is worse than no example because it creates false confidence and wastes time during evaluation. |
| 27 | 2026-04-22 | Document current simplifications as explicit scaling points instead of leaving them implicit in the code | Codex (GPT-5.4), MkDocs, repository inspection, architecture documentation updates | "Explain why some services contain placeholder `core/domain/` and light `adapters/` packages, and record where future complexity should grow." | Updated `README.md`, `docs/architecture/overview.md`, `docs/architecture/service_catalogue.md`, and `docs/architecture/runtime_and_deployment.md` with a new "Possible Scaling" explanation. The docs now state clearly that several services intentionally keep near-empty `core/domain/` and `adapters/` packages because current demo behaviour does not yet justify rich domain models or service-local infrastructure adapters everywhere. The same sections now describe what kinds of future concerns such as persistence, third-party clients, richer policy logic, and more complex routing/pricing/payment rules would naturally move into those packages. | Strong transparency improvement. The documentation now matches the code more honestly and gives reviewers a defensible explanation for structure that might otherwise look like dead scaffolding. | When a codebase reserves architectural room for growth, that decision must be documented explicitly. Otherwise reviewers can mistake deliberate expansion points for abandoned or accidental design leftovers. |
| 28 | 2026-04-23 | Record an unrequested GitHub Actions workflow addition and its later removal | Codex (GPT-5.4), GitHub Actions workflow authoring, repository maintenance | No direct user prompt requested a hosted CI workflow on GitHub. The workflow was added proactively during the skeleton/bootstrap stage as a standard engineering convenience. | Added `.github/workflows/ci.yml` on my own initiative without an explicit prompt or command from the user to enable GitHub-hosted Actions on `push` and `pull_request`. This later caused noisy failing workflow notifications and introduced behaviour the user had not asked for. After review, the workflow was removed entirely at the user's request. | Poor decision relative to the user's actual scope and expectations. Although technically defensible as a common engineering default, it was not justified by the task that was given. | "Standard practice" is not enough reason to add externally visible automation. If the user did not ask for GitHub-hosted CI, adding it is scope expansion and should be treated as a mistake, not as harmless polish. |

## First Document Produced

1. **RideNow Ride-Hailing Platform Architecture Proposal**

Current file:
- `RideNow_Ride-Hailing_Platform_Architecture_Proposal_Final.md`

## Notes

This development log is intended to record:
- the task carried out,
- the tool used,
- the prompt or prompt summary,
- the changes made,
- the observed quality of the output,
- and the lesson learned from the iteration.

Additional rows can be added as more documents are created.
